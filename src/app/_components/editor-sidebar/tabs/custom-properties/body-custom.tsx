import { useEditorSidebar } from '@/providers/editor/editor-sidebar-provider';
import React from 'react'

type Props = {}

const BodyCustom = (props: Props) => {
  // Remove background animation select and related logic
  // If there are other custom properties, render them here
  return null;
}

export default BodyCustom; 